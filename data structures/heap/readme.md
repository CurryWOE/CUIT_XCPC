# 堆
堆是一类数据结构的统称，形态上是一棵有根树，其每个节点都有一个键值，且每个节点的键值都大于等于/小于等于其父亲的键值。每个节点的键值都大于等于其父亲键值的堆叫小根堆，否则叫大根堆

堆常被用来实现优先队列

堆有很多种实现：二叉堆，配对堆，左偏树，二项堆，斐波那契堆

std::priority_queue是基于二叉堆实现的优先队列，默认大根堆

>小技巧，std::priority_queue的构造函数比N次插入更块

在Dijkstra和Prim，二叉堆最优，二项堆和斐波那契堆只是理论复杂度更优，但实际效率不行